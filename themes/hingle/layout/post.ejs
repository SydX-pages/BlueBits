<main>
    <div class="wrap min">
        <section class="post-title">
            <h2><%= page.title ? page.title : "没有标题的文章" %></h2>
            <div class="post-meta">
                <time class="date"><%= date(page.date, "YYYY.MM.DD") %></time>
            <% if (page.categories && page.categories.length){ %>
                <span class="category"><%- list_categories(page.categories, {
                    show_count: false,
                    class: "",
                    style: "none",
                    separator: "，"
                    }) %></span>
            <% } %>
            </div>
        </section>
        <article class="post-content">
        <% if (moment().unix() - page.date.unix() >= 15552000) { %>
            <blockquote>这篇文章上次修改于 <%= Math.ceil((moment().unix() - page.date.unix()) / 86400) %> 天前，可能其部分内容已经发生变化，如有疑问可询问作者。</blockquote>
        <% } %>
            <%- page.content %>
        </article>
        <section class="post-near">
            <ul>
                <% if( page.prev !== undefined) { %>
                    <li>上一篇: <a href="<%= url_for(page.prev.path) %>"><%= page.prev.title %></a></li>
                <% } else { %>
                    <li>上一篇: 看完啦 (つд⊂)</li>
                <% } %>
                <% if( page.next !== undefined) { %>
                    <li>下一篇: <a href="<%= url_for(page.next.path) %>"><%= page.next.title %></a></li>
                <% } else { %>
                    <li>下一篇: 看完啦 (つд⊂)</li>
                <% } %>
            </ul>
        </section>
        <% if (page.tags && page.tags.length){ %>
            <section class="post-tags">
            <%- list_tags(page.tags, {
                show_count: false,
                class: "",
                style: "none",
                separator: ""
                }) %>
            </section>
        <% } %>
    <% if (theme.author && theme.author_text) { %>
        <section class="post-author">
        <% if (theme.author_avatar) { %>
            <figure class="author-avatar">
                <img src="<%- theme.author_avatar %>" alt="<%= theme.author %>" />
            </figure>
        <% } %>
            <div class="author-info">
                <h4><%= theme.author %></h4>
                <p><%- theme.author_text %></p>
            </div>
        </section>
    <% } %>
    
<style>
  /* 评论系统选项卡样式 */
  .comment-tabs {
    display: flex;
    justify-content: center;
    margin: 20px 0;
    border-bottom: 2px solid #ccc;
  }

  .comment-tab {
    padding: 10px 20px;
    cursor: pointer;
    border: 1px solid #ccc;
    border-bottom: none;
    border-radius: 5px 5px 0 0;
    margin: 0 5px;
    background-color: #f5f5f5;
    transition: background 0.2s;
  }

  .comment-tab.active {
    background-color: #fff;
    font-weight: bold;
    border-color: #888;
  }

  /* 评论内容容器 */
  .comment-container {
    border: 1px solid #ccc;
    padding: 20px;
    border-radius: 0 5px 5px 5px;
    background-color: #fff;
  }
</style>

<!-- 选项卡 -->
<div class="comment-tabs">
  <div class="comment-tab active" onclick="switchComment('valine', this)">FreeToSay</div>
  <div class="comment-tab" onclick="switchComment('giscus', this)">GithubOnly</div>
</div>

<!-- 评论内容 -->
<div class="comment-container">
  <!-- Valine 评论区 -->
  <div id="valine-comments">
      <div id="vcomments"></div>
      <script src="https://cdn.jsdelivr.net/npm/valine/dist/Valine.min.js"></script>
      <script>
          new Valine({
              el: '#vcomments',
              <!--  国际版
              appId: '3A8hFNJdo8zM3HRx5OGG8pX4-MdYXbMMI',
              appKey: 'myov7wtXnCt5YeCQotsZp6lh',
              serverURLs: 'https://3a8hfnjd.api.lncldglobal.com',
              -->
              appId: 'ZLjGZsBcGkFEVQz10YzaahvK-gzGzoHsz',
              appKey: 'hYCU79M5VL6WKap3CjynSocY',
              serverURLs: 'https://zljgzsbc.lc-cn-n1-shared.com',
              placeholder: '欢迎留言~',
              avatar: '',
              visitor: true
          });
      </script>
  </div>

  <!-- Giscus 评论区 -->
  <div id="giscus-comments" style="display:none;">
      <script src="https://giscus.app/client.js"
          data-repo="SydX-pages/BBcomments"
          data-repo-id="R_kgDOP3_yLA"
          data-category="Comments"
          data-category-id="DIC_kwDOP3_yLM4Cv9ii"
          data-mapping="pathname"
          data-strict="0"
          data-reactions-enabled="1"
          data-emit-metadata="0"
          data-input-position="bottom"
          data-theme="light_protanopia"
          data-lang="zh-CN"
          crossorigin="anonymous"
          async>
      </script>
  </div>
</div>

<script>
  function switchComment(type, tab) {
    const valineBox = document.getElementById('valine-comments');
    const giscusBox = document.getElementById('giscus-comments');
    
    // 切换显示
    valineBox.style.display = (type === 'valine') ? 'block' : 'none';
    giscusBox.style.display = (type === 'giscus') ? 'block' : 'none';

    // 切换 active 样式
    const tabs = document.querySelectorAll('.comment-tab');
    tabs.forEach(t => t.classList.remove('active'));
    tab.classList.add('active');
  }
</script>
</div>
    
</main>
